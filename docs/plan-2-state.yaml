# The only hand-written input to the tracker.
#
# ONE LINE PER ITEM, because a status somebody decided is the one thing a machine cannot derive.
# Everything else on the page (timings, the fact inventory, budget movements, the open residue) is
# measured by adapters/tracker.py from the tree, so it cannot be claimed better than it is.
#
# state: todo | doing | done | dropped
# note:  what changed, or why it was dropped. Required for `done` and `dropped`.
#
# A `done` with no note fails `./adapters/tracker.py --check`, which the gate runs. That is the
# smallest guard against the fault this file exists to prevent: the first round's tracker showed a
# step complete that had never been started.

generated_by: adapters/tracker.py
plan: docs/REFACTOR-PLAN-2.md

stages:
  A:
    title: The cycle stops re-answering itself
    items:
      - id: A1
        title: A green-tree token, hashing what the checks read
        state: done
        note: >-
          adapters/greentree.py.
          Hashes every tracked file, all of data/build and the deployed site data and interface: 4,370 files in 0.19 s, so over-approximating costs nothing and being clever is how the set comes to be wrong.
          Refuses itself if the tree moved, if it covers a different set, or if it records a different run, and it excludes itself from its own hash because writing it invalidated it.
          Push skips 127 s.
      - id: A2
        title: deploy.sh stops running the checks
        state: done
        note: >-
          deploy.sh ran every check after copying so the published report would not claim violations the copy had fixed.
          Three invariants read the deployed tree, derived from which ones name SITE and not from the comment, which said two.
          check.py --deploy-window re-answers those and patches checks.json.
          37 s to 1 s; deploy 39 s to 3.7 s.
      - id: A3
        title: A build skips the checks by default
        state: done
        note: >-
          A build now skips the checks unless --checks is passed.
          The flag existed and went unused, which was a sign the default was wrong.
          build.py 79 s to 41.7 s.
          The hooks run check.py --gate, which ignores both flags.

  B:
    title: The expensive checks stop rescanning what has not changed
    items:
      - id: B1
        title: stock phrasing in comments caches per file
        state: todo
      - id: B2
        title: deploy.sh regenerates only pages whose content changed
        state: todo
      - id: B3
        title: Per-check timings, so the next slow one is visible
        state: todo

  C:
    title: The store becomes load-bearing
    items:
      - id: C1
        title: Nothing writes to the store except the compiler, proved by a lint
        state: todo
      - id: C2
        title: The cascade is gated on output change, never on input change
        state: todo
      - id: C3
        title: Convergence to a fixed point, which is the equivalence argument
        state: todo
      - id: C4
        title: Corpus-wide reductions recomputed freely, their digests deciding the cascade
        state: todo
      - id: C5
        title: A focused test per delta kind, including delete and retract
        state: todo
      - id: C6
        title: Differential site build, keyed on the digest of the rows an artefact renders
        state: dropped
        note: >-
          Measured before starting it. Writing every page costs 0.8 s: stubs.py writes 3,048 work
          pages and pages.py writes 2,402 credit and publisher pages, unconditionally, in under a
          second. The 39 s I attributed to page generation was check.py --runtime, the fourth run
          of the checks in one cycle. Dropped as effort against eight tenths of a second.
      - id: C7
        title: A weekly CI run of the cache-free path, compared against the incremental result
        state: todo
      - id: C8
        title: The eight researched readings carrying no reasoning
        state: todo
      - id: C9
        title: analyser gets a ruling or a recorded reason
        state: todo

  D:
    title: What the census found, and the facts the first round did not reach
    items:
      - id: D1
        title: A name's comparison key has three incompatible answers across thirteen folds
        state: done
        note: >-
          DECIDED, not deferred.
          There are two questions and thirteen functions were answering both under one name.
          `facts/namekey.fold` is the IDENTITY key: NFKC and the space, which are typography and nothing else.
          Measured over 6,076 surfaces: the space merges 108 pairs and every one is one person, 源 久也 and 源久也.
          Case merges 4, the interpunct 3, brackets 57, and each of those can also merge two things that are not one.
          The project already ruled that a wrong join erases a person while a wrong split invents one, so the identity key takes only typography and every judgement lives in `loosely`, which a caller opts into by name.
          Four identity folds now delegate and agree on all 6,076 surfaces; check.py held three more copies and now asks.
          Five that answered the OTHER question are renamed `match_key`, because the shared name is what produced thirteen answers, and they stay separate: they are genuinely different rules, one stripping bracket characters and keeping the contents where another strips the span.
          Two were never name keys at all and are now `width`; one folded rows into a document and is `fold_in`.
      - id: D2
        title: Six distinct implementations of kata, kana to katakana
        state: done
        note: >-
          adapters/lint/duplicates.py, registered as the budget `facts with more than one home` at 35.
          It looks for a string vocabulary, a dict shape, a regex or a function body in two files.
          Its own docstring says what it cannot see, which is most of the problem: a rule written as different code twice is invisible, and the thirteen folds were found by this while their DISAGREEMENT was found by running them.
      - id: D3
        title: Twelve regexes answering "is this Japanese", three of them disagreeing on kana
        state: done
        note: >-
          facts/script owns four questions that thirty regexes were sharing: has_kana, has_kanji, has_script and has_typography.
          Measured: the two main "is this Japanese" classes agree on all 6,076 surfaces, so extension A and the compatibility ideographs never bite here; check.JAPANESE differs on 18 and is asking a different question, since ＦＬＯＷＥＲＣＨＩＬＤ is Japanese typography with no Japanese letter.
          One was simply wrong: ・ sits at U+30FB inside the katakana block, so two kana tests called flower・flower kana.
          A separator is not a letter.
          The letter tests span letters and the safety net spans whole blocks, because a miss on an English page is visible to a reader.
          My own widening spanned Hangul and Yi, because the literal 豈 is U+8C48 and the compatibility block begins at U+F900 with a different character that looks the same; test_interface caught it by asking whether 싱 is Japanese.
          Five callers converted, 24 narrower patterns left alone as rules about one source each.
      - id: D4
        title: Three owned facts have copies elsewhere, plus a KANA_SURFACE I duplicated myself
        state: done
        note: >-
          Four copies of an owned fact removed.
          build.py held the reading-basis precedence and build.py and names/store.py both held the English-basis precedence, each keyed on a vocabulary its fact already owned; the ranks are columns of those tables now, with the reasoning beside the bases they rank.
          KANA_SURFACE was mine, left in check.py when I moved a check into facts/division this week, and the lint found it the same week.
      - id: D5
        title: YURI_TAGS, three homes, and it decides inclusion
        state: done
        note: >-
          facts/marketing owns {百合, GL, ガールズラブ}.
          It had three homes across pixivcomic and two kadokomi adapters, so three could have drifted about what admits a work.
          Deliberately narrow: 百合姫 is a magazine and admitting on a magazine name is a different argument.
          BLINDSPOT.md records that a platform using another word is invisible and will be reported as absence.
      - id: D6
        title: Six smaller vocabularies with two homes each
        state: done
        note: >-
          The remaining small vocabularies given one home.
          romanisation still had its styles typed in credits.py twice and romfloor.py once, four homes for a fact marked owned.
          The name kinds and the two a naming pass can research are `facts/namekey.KINDS` and RESEARCHABLE_KINDS, and adapters/store keys its map on them rather than retyping.
      - id: D7
        title: kadokomi work_data, the one exact duplicate function body
        state: done
        note: >-
          kadokomi/confirm.work_data was byte-identical to kadokomi/releases.work_data, the one exact duplicate body in the tree.
          confirm asks releases now, so a change to the page lands once.
      - id: D8
        title: Fifteen scraping patterns written twice or more
        state: done
        note: >-
          adapters/htmlbits.py owns the patterns every web source shares: the document title, og:title, the RSS and Atom wrappers, a link, alt text and a definition pair.
          Two spellings were in use for the title and two for og:title, and the tolerant form of each keeps what both callers had, so a title broken over a line now reads where one spelling missed it.
          A selector naming one shop stays with that shop: four of them in a shared file would put four shops HTML somewhere none of them owns.
          A mechanical pass rewrote the duplicates lint own canary into a shared constant, making it correct and useless, and the self-test caught it.
      - id: D9
        title: A lint for a fact with a second home, so the census cannot go stale
        state: done
        note: >-
          adapters/lint/duplicates.py runs in the gate as the budget `facts with more than one home`, 35 to 12.
          docs/duplicates-allowed.yaml carries the cases somebody argued, so the number is a list of open ones and not a wall: four record shapes two adapters legitimately both emit, each saying what would make it a divergence again.
          One entry says plainly that the argument has NOT been made, for the engine registry in editions/capture and editions/engines, because an allow-list that quietly absorbs what you meant to fix is worse than none.
          Its self-test keeps a literal duplicate on purpose, since a canary is the one place a duplicate belongs.
      - id: D10
        title: title cataloguing, which already has one reader
        state: todo
      - id: D11
        title: imprint, which already has one reader
        state: todo
      - id: D12
        title: work identity
        state: todo
      - id: D13
        title: dates
        state: todo
      - id: D14
        title: inclusion
        state: todo
      - id: D15
        title: Rendering surfaces derived from the renderer, not listed
        state: todo

  E:
    title: The residue the first round recorded and did not clear
    items:
      - id: E1
        title: Eight comments asserting an impossibility with nothing to prove it
        state: done
        note: >-
          Eight to zero, and three of the eight were the instrument and not the comment.
          Two were past narrative caught by a bare `never reached`: a fix to the renderer never reached what it had already rendered, and 縁山 never reached a row.
          Those are transitive past tense, so the phrase now wants a copula, and the self-test keeps both beside "this branch is never reached" so the narrowing is held to having lost nothing.
          A third was the evidence pattern reading `proved` and `proven` but not `proves`, which reported two comments that named the assertions directly below them.
          Of the five real ones, two named the four cases in test_credits that hand the analyser a reader returning マチガイ for every name, and one named the assertion beneath it.
          TWO WERE WRONG AND WERE CORRECTED RATHER THAN EVIDENCED. `no-print-edition` was called a count that will never fall, but a web serial that later gets a tankōbon leaves it; what is true is that no pass moves it, because the change arrives from a capture finding the printing.
          The tics CJK exemption said a decorative emoji in our own English would never share a line with Japanese; it now says what the exemption costs, and the self-test pins both sides.
          Also removed a comment block duplicated verbatim in the claims lint itself.
      - id: E2
        title: Two modules called store
        state: todo
      - id: E7
        title: Compiled bytecode was inside the green-tree token's covered set
        state: done
        note: >-
          Two .pyc were tracked from before the ignore rule, so merely importing build or crossplatform rewrote a file the token hashes and it was refused for a tree nobody had touched.
          Found while triaging the worktrees, every one of which showed as dirty for that reason alone.
          Untracked, and the covered set now excludes compiled files, which the self-test asserts against the real set rather than a fixture: untracking two files is not a rule.
      - id: E3
        title: The glued-romanisation shape, Uedakyōko
        state: todo
      - id: E4
        title: Kana spelling a name from another language, Sutefan Sejiku
        state: todo
      - id: E6
        title: The three branches holding work main never took
        state: todo
      - id: E5
        title: Fifteen leftover worktrees
        state: doing
        note: >-
          Triaged, not yet removed: removing a worktree is denied to me here and needs the user to run it.
          Fourteen worktrees, twenty-one work branches. Eighteen branches are fully contained in main and their worktrees hold nothing but recompiled bytecode drift, so both go.
          THREE CARRY WORK THAT IS NOT IN MAIN, measured by test-merge rather than assumed: work/ichijinsha holds adapters/ichijinsha.py at 698 lines with its tests and 543 captured source pages, and conflicts with main in check.py alone; work/q10 holds the byline division work and conflicts in credit_identity, pages and build; work/romfallback holds adapters/names/organisations.py and conflicts in creditline, inputs and pass4_analyser.
          Every other conflict is a generated artefact, so a merge takes main's and rebuilds.
          Main reached ルミナス＝ブルー by a narrower route, isbd.RULED, so the 一迅社 capture is not superseded and is the one piece here that would cost re-fetching.
          Merging them is E6 and not this item: it changes what the corpus contains, which does not belong inside tree hygiene.

  F:
    title: The common case stops re-verifying what did not change
    items:
      - id: F1
        title: Test selection from COVERS, which every suite already declares
        state: todo
      - id: F2
        title: Incremental verification, each check declaring the inputs it reads
        state: todo
      - id: F3
        title: Incremental build, stages keyed on the digest of their inputs
        state: todo
      - id: F4
        title: The gate and the tests run concurrently, being independent of each other
        state: todo
      - id: F5
        title: A cache-free --full path, which the scheduled CI run exercises end to end
        state: todo
